# PauseAI Website - Developer Notes (Anthony)

This file is symlinked as CLAUDE.md (gitignored) so Claude Code reads it.
Convention: each developer can have their own `.{username}.CLAUDE.md` tracked in the repo.

## Project Overview

SvelteKit website for PauseAI. Uses Paraglide for i18n/l10n.

## Tech Stack

- SvelteKit
- Paraglide (localization)
- pnpm
- Node 24.2.0 (see .nvmrc)
- Deployed via Netlify

## Key Directories

- `src/` - SvelteKit source
- `messages/` - Paraglide translation files
- `l10n-cage/` - Localization tooling
- `scripts/` - Build/utility scripts

## Dev Server

Port is explicitly set to **37572** in vite.config.ts (not Vite's default 5173).

## Session Notes

(Add session-specific context here as we work)
